apiVersion: v1
kind: Service
metadata:
  name: {{ include "laa-test-a-provider-data-ui.fullname" . }}
  labels:
    {{- include "laa-test-a-provider-data-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laa-test-a-provider-data-ui.selectorLabels" . | nindent 4 }}